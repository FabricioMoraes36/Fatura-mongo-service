# Como testar a aplicação ?

É necessario ter o mongo rodando então use o comando:
docker run -d --name mongo-curso -p 27017:27017 mongo:latest

Dentro do proprio projeto existe a pasta postman com o arquivo da collection com as rotas, é so jogar pro postman e testar
